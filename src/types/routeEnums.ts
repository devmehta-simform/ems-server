const enum Routes {
  'user' = '/user',
  'event' = '/event',
}

const enum UserRoutes {
  'login' = '/login',
  'register' = '/register',
}

const enum EventRoutes {
  'event' = '/',
}

export { Routes, UserRoutes, EventRoutes };
